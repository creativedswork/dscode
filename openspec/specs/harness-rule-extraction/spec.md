# harness-rule-extraction Specification

## Purpose

[DEPRECATED] This spec has been fully replaced by LLM-driven rule attribution (`attributeWithLLM`) in the `eval-llm-rule-attribution` spec. All deterministic rule extraction (extractRules, detector registry, CHIFF→Rule mapping, de-concretization) has been removed. Rule generation now goes exclusively through the LLM in Step 7.

## Requirements

No active requirements. All previous requirements have been removed and migrated to `eval-llm-rule-attribution`.
