## 1. CSS Token Definition

- [x] 1.1 Add `--color-accent-bg` to `:root` in `web/src/index.css` with light-mode value `rgba(202, 138, 4, 0.12)`
- [x] 1.2 Add `--color-accent-bg` to `.dark` in `web/src/index.css` with dark-mode value `rgba(212, 151, 8, 0.15)`

## 2. Verification

- [ ] 2.1 Manual test: open the web UI, click different sessions in the sidebar, verify the active session row shows a visible amber-tinted background
- [ ] 2.2 Manual test: toggle dark mode, verify the active session background adapts to the dark theme tint
- [ ] 2.3 Manual test: confirm only one session is highlighted at a time when switching sessions
- [ ] 2.4 Manual test: confirm the active session stays highlighted during agent processing (no opacity dimming)
