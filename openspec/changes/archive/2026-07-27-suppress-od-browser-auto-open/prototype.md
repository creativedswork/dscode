## Prototype Status

No prototype needed — non-UI change: adding a single CLI argument (`--no-open`) to a child process spawn call in `src/core/od-daemon.ts`. No user-facing visual components, pages, or interactions are affected.
