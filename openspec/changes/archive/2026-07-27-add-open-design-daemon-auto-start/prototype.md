## Prototype Status

No prototype needed — non-UI change; adds `.env.example`, `.gitignore` entry, and `--with-od` CLI flag with backend daemon lifecycle management. No visual or interaction component changes.
