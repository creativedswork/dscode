# 已实现方案归档

本目录保存已落地功能的分析、方案和实现时设计记录。它们解释“为什么这样做”，
但不再维护为当前 API 或行为文档。

当前行为以对应的 `openspec/specs/`、代码和 `docs/` 根目录权威文档为准：

- `ui/` — Chat → Dashboard transition 历史方案；
- `eval/` — CHIFF iterative focusing 与 recovery arc 分析；
- `edit-tool-resilience/` — Edit Tool 失败分析和治理方案。
